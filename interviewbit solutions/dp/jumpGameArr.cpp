#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
#define vi vector<int>

int answer(vi a, int be){
  if(be == a.size()-1) return 0;
  for(int i = 0; i < a.size(); i++){
    for(int j = )
  }
}

int main(){
  vi a = {1,0,0,0,4};
  cout<<answer(a, 0);

}
